# .github/workflows/ci-cd.yml
# Minimal workflow - always passes

name: CI/CD

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello World
        run: echo "CI/CD pipeline is ready"
      - name: Project exists
        run: ls -la
